#include "helper.h"
#include <math.h>
#include <stdio.h>


// imprime os elementos do vetor separados por "\n"
void print_vec(double* x, int n) {
    for (int i = 0; i < n; ++i) {
        printf("%f\n", x[i]);
    }
}


